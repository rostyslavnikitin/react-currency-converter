import React from "react";
import { render, screen } from '@testing-library/react';
import App from './App';
//
// test('renders converter link', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/converter/i);
//   expect(linkElement).toBeInTheDocument();
// });
